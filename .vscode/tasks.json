{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "sbt Build and Package",
            "type": "shell",
            "command": "sbt package",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}